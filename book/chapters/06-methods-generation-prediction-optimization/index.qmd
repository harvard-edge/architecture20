# Method Roles Under Feedback Limits {#sec-methods-generation-prediction-optimization}

::: {.epigraph}
> *"But they are useless. They can only give you answers."*
>
> — Pablo Picasso, *The Paris Review* (1964)
:::

::: {.column-margin}
**Author's Note:** Picasso was speaking about calculating machines. The remark captures the exact constraint on AI methods in architecture. An AI system can produce an output but cannot confer authority on it, so a method role is useful only when the loop can measure, reject, or bound that output. Search and reinforcement learning need measurable objectives; critique and repair need evidence that can reject their claims.
:::

```{=latex}
\abstract*{Architecture 2.0 needs a method-selection framework for AI-assisted and agentic architecture loops, not a catalog of algorithms. Generation, prediction, optimization, critique, repair, verification, explanation, and coordination are roles inside a design loop. Which role matters depends on the task, representation, environment, feedback budget, evidence burden, and cost of being wrong.}
```

::: {.callout-crux}
Which method role does the loop need, what feedback can support it, and what
authority must remain outside it?
:::

@sec-architecture-environments-tool-interfaces defined the environment in which methods act and receive feedback.
This chapter examines the method-role and feedback-budget fields of the
design-loop card. Current model rankings will age; the useful question is which
method role matches the architecture work and available feedback. Because
current systems can draft artifacts, call tools, revise results, and recommend
experiments, each study must state what a method may do and what decisions
remain with the architect.

Capability and authority answer different questions. Capability asks what a
method can generate, estimate, check, repair, explain, or route. Authority asks
what the loop permits that output to change or commit. A successful tool call,
prediction, or verification step demonstrates a capability within its tested
scope. It does not authorize a stronger architectural commitment.

A model that can generate plausible schedules,
configuration files, code fragments, RTL snippets, or design prose may be
useful for drafting alternatives, but weak for choosing among them. A
surrogate may predict latency or energy well inside a calibrated accelerator
design region, but fail outside the sampled space. Bayesian optimization[^fn-ml-bo-acquisition-policy] may
spend scarce simulator or synthesis evaluations carefully, but only if the
action space and objective are well formed. Reinforcement learning[^fn-rl-delayed-feedback] may be
attractive for placement, scheduling, or adaptive control, but dangerous when
invalid actions are common and feedback is delayed. A critic may be more
valuable than a generator when the urgent problem is exposing missing workload
evidence, not proposing more candidates.

[^fn-ml-bo-acquisition-policy]: In this chapter, Bayesian optimization matters as the acquisition policy that chooses which scarce simulator or synthesis evaluation to spend next. Its value depends less on the method name than on a well-formed action space, objective, uncertainty model, and escalation rule.

[^fn-rl-delayed-feedback]: For these method roles, the RL issue is delayed and invalid feedback. Placement, scheduling, and adaptive-control loops can make RL attractive, while illegal moves or late rewards make the loop's rejection checks decisive.

Architecture 2.0 therefore treats methods as roles in a compound system.

> **Method role.** A method role is the specific job a model, search procedure,
> tool, script, or person performs inside a design loop, together with the state
> it may read, the actions it may take, the feedback it receives, and the
> authority its output does not exceed.

> **Feedback budget.** A feedback budget is the amount, latency, cost, and
> fidelity of evaluation the loop can spend across tool runs, measurements,
> failed attempts, and review.

Generation proposes candidates. Prediction estimates candidate properties.
Optimization chooses what to evaluate next. Critique finds flaws, repair fixes
malformed artifacts, explanation makes evidence traceable, and coordination
routes state and work. Verification appears here only as a checking role;
@sec-feedback-verification-trust covers the broader rejection and evidence
process.

Choose a method according to the job the study needs, the feedback available,
and the evidence required to reject or accept its output, rather than according
to an overall model ranking. Reviewers should still ask what state the method
can see, what actions it may take, and what evidence forces it to stop or
escalate.

Tensor-program autotuning gives a small version of this role split. AutoTVM,
introduced in @sec-design-loop-no-longer-scales, and the follow-on
tensor-program optimizer Ansor generate candidate schedules, predict
or rank promising regions, spend measured hardware feedback selectively, and
reject candidates that fail correctness or performance checks
[@ChenEtAl2018AutoTVM; @ZhengEtAl2020Ansor]. The method is useful because the
loop assigns roles and rejection checks, not because search alone is an architectural
claim.

Machine learning for architecture did not begin with recent generative methods.
Regression models and learned surrogates predicted performance or power, while
predictive DSE and autotuners searched defined spaces inside methodologies whose
tasks, tools, workloads, and acceptance rules were specified by people
[@LeeBrooks2006RegressionModeling; @IpekEtAl2006PredictiveDSE;
@ChenEtAl2018AutoTVM; @ZhengEtAl2020Ansor]. The perceptron branch predictor is
an instructive learned mechanism in this lineage [@JimenezLin2001Perceptron].
Its role sits at the cheap extreme of the feedback budget, where retirement
grades every prediction against ground truth within cycles and updates predictor
state from the outcome. No design-time role sees feedback that cheap, which is
why this book takes the study, not the component, as its unit of analysis. In
those examples, learning
operated inside the artifact or as a scoped predictor or optimizer within a
human-specified methodology. Current systems can also
generate or edit artifacts, call tools, choose experiments or workload slices
when an environment provides those actions, route work among roles, and thereby
influence which candidate and claim reach review [@KrishnanEtAl2023ArchGym;
@OuyangEtAl2025KernelBench; @BlockloveEtAl2023ChipChat]. The difference is not
that older work lacked intelligence or rigor. Current systems can automate more
of the work leading to a reviewed claim. A method description must therefore
state the actions it may take, the feedback it receives, and the decisions it
is not authorized to make.

The chapter first bounds method authority and hardware awareness, then examines
the major method roles before turning to method selection under constraints.

::: {.callout-learning-objectives}
After this chapter you can turn "we used AI method X" into the method-role view of
the design-loop card.
That means you can:

- **Match** an AI method role (generate, predict, optimize, critique, repair, verify,
  explain, or coordinate) to an architecture task;
- **Separate** what a method can do from what its output is authorized to decide or commit;
- **Allocate** scarce feedback across generation, prediction, optimization, critique, and repair;
- **Choose** a method by its object, feedback, rejection path, and loop bottleneck rather than by fashion.
:::

## Match the Method to the Architecture Task

The first step is to name the architecture task. Design-space exploration,
workload characterization, benchmark construction, code generation, RTL
repair, compiler/runtime tuning, accelerator search, chiplet partitioning,
physical-design assistance, and evidence critique are not the same problem.
They require different state, allow different actions, tolerate different
errors, and require different feedback.

This is why environment work such as ArchGym, the worked example of
@sec-architecture-environments-tool-interfaces, matters here. It makes method
comparisons meaningful by defining tasks, actions, observations, workloads,
and feedback [@KrishnanEtAl2023ArchGym]. But even a shared environment
does not decide which method role is appropriate. The role depends on what
the study is trying to accomplish. A single architecture study can run
several method roles at once, such as generators to propose novelty and
predictors to estimate cost, but each role assumes a different claim and
has a different burden of evidence.

![**Method roles in an architecture study.** Generation, prediction, optimization, critique, repair, verification, explanation, and coordination each need recorded state, feedback, rejection criteria, and a decision boundary. Their outputs inform review by the role authorized to accept, reject, escalate, or revise the claim.](images/F7-method-role-map){#fig-method-role-map width="100%" fig-alt="Role map showing generation, prediction, optimization, critique, repair, verification, explanation, and coordination as defined roles inside an architecture study, with their outputs feeding review by an authorized decision holder."}

Read @fig-method-role-map as a role map, not as a claim that every study needs
every role. A design-space search may use a generator to propose candidates, a
predictor to rank them cheaply, and independent tool checks to reject invalid
results. The method-role field records those assignments and their limits.

Each role must be evaluated against evidence that can reject its output. A successful output does
not receive decision authority on that basis, as @tbl-method-role-limits records.
The design-loop card stores these roles in its method-role field, and
the machine-checkable schema in @sec-appendix-b-design-loop-card documents how
each role maps onto the card's role enum.

| **Role** | **Permitted action** | **Evidence or rejection** | **Authority limit** |
| --- | --- | --- | --- |
| Generate | Propose configurations, code, RTL fragments, tests, reports, or hypotheses. | Parsers, compilers, tests, constraints, provenance, and review. | A valid proposal is not an accepted design. |
| Predict | Estimate performance, energy, area, latency, reliability, or cost. | Calibration, support region, uncertainty, held-out checks, and counterexamples. | A calibrated estimate can rank or escalate only inside its tested scope. |
| Optimize | Choose the next candidate, region, or experiment to evaluate. | Declared objective, legal actions, cost model, feedback budget, and stopping rule. | A sampling policy does not own the resulting architecture decision. |
| Critique | Find weak assumptions, missing evidence, unsupported claims, or invalid actions. | Artifact provenance, comparison baseline, counterexamples, and unresolved failures. | A critique may trigger rejection or escalation only through the declared review policy. |
| Repair | Propose corrections to malformed artifacts. | Original artifact, patch or diff, rerun checks, and remaining failures. | Repair may correct form; it may not waive a violated design constraint. |
| Verify | Check invariants, tests, tool outputs, and evidence records. | Reproducible checks, provenance, independence basis, and escalation rules. | A passing check supports only the property and scope it tested. |
| Explain | State tradeoffs, failures, uncertainty, and rejected alternatives clearly. | Traceable evidence, assumptions, contrast cases, and stated limits. | A persuasive account adds no authority beyond its evidence. |
| Coordinate | Route state, tasks, tool calls, and evidence among role-specific participants. | Shared state, actor permissions, versioned records, and stop or escalation rules. | Routing work cannot silently expand or combine decision authority. |

: **Each method role needs different evidence:** Generation, prediction, optimization, critique, repair, verification, explanation, and coordination make different claims and therefore require different rejection checks. {#tbl-method-role-limits tbl-colwidths="[18,28,25,19]"}

The last column states each role's limit. Demonstrated capability remains
narrower than decision authority.

::: {.callout-engineer-move title="State every AI method claim concretely"}
For every method, state the artifact or parameter it changes, the interface it
uses, the feedback it receives, the check that can reject its output, and the
person responsible for the resulting decision. A concrete sentence might read,
"The optimizer proposes array geometries, SCALE-Sim evaluates them, and the
study owner decides whether any candidate warrants further analysis."
:::

The same capability can support very different authority depending on the
object and consequence. @tbl-method-object-authority makes that distinction
concrete without defining another role taxonomy.

| **Capability** | **Permitted within the role** | **Does not authorize** |
| --- | --- | --- |
| Generate | Draft benchmark questions, where editorial review is check enough, or RTL candidates, where physical-design consequence demands tool rejection before ranking. | Treating either draft as a verified artifact or architecture decision. |
| Predict | Rank candidates or trigger escalation inside a calibrated support region. | Claiming final power, timing, reliability, or deployment behavior. |
| Optimize | Select the next proxy, simulation, synthesis, or measurement to spend. | Committing the winning candidate or changing the declared objective. |

: **The object and consequence bound method authority:** The same capability may draft, rank, or route within scope without acquiring authority to verify, waive, or commit. {#tbl-method-object-authority tbl-colwidths="[24,36,30]"}

In every row, demonstrated capability remains narrower than decision authority.
The same action may be safe within a proposal right and unsafe when it changes a
design without approval.

## Hardware Awareness as Observable Capability

Naming the architecture object a method touches raises a prior question,
whether the method actually understands the hardware behind that object, or
only borrows its terms. The capability view that follows asks, concretely for
hardware, what feedback can support the role. A hardware-awareness claim is
limited by the feedback that can reject the method's output. Hardware awareness
is not the same as using hardware vocabulary. A generated proposal can mention
caches, vector units, power targets, or a process node and
still be unaware of the architectural consequences of those terms.

> **Hardware awareness.** In this chapter, hardware awareness means that a
> method or AI-assisted system can represent hardware-relevant constraints, act within a valid
> hardware/software design space, obtain feedback from appropriate tools or
> measurements, and report evidence that allows its output to be rejected.

Methods increasingly generate or revise kernels, compiler settings,
accelerator configurations, RTL fragments, placement constraints, and design
reports. Hardware-aware neural architecture search[^fn-ml-nas], in the ML
model-design sense, already shows the value of placing latency, energy, memory,
and device cost inside the search problem [@BenmezianeEtAl2021HardwareAwareNAS].
Here the test is broader. What hardware-relevant action can the method take, and
what evidence can reject it?

[^fn-ml-nas]: Neural architecture search (NAS) automates the design of neural network architectures. This chapter treats it as a cautionary adjacent field, one whose search only becomes reusable when the search space, data, training budget, evaluation rule, and rejected alternatives are all recorded.

Vocabulary, constraints, performance models, tools, evidence, and
authority-boundary awareness form six independently testable capabilities. A tool wrapper may compile and profile without having a
calibrated performance model, while a surrogate may estimate uncertainty
without controlling a tool. No capability grants a decision right merely by
being demonstrated.

The same boundary applies to proxies. The proxy mismatch first named in
@sec-architecture-environments-tool-interfaces becomes a Goodhart-style failure
when a method optimizes the proxy.
@sec-feedback-verification-trust develops the evidence response.

::: {.callout-architect-checkpoint title="The Escalation Boundary"}
When a method reaches the limit of its evidence or encounters a constraint it
cannot resolve, it must stop or escalate to the declared decision owner. Naming
uncertainty and preserving failed runs and rejected alternatives make escalation possible; neither
authorizes the method to waive the constraint or commit the result.
:::

Functional correctness is the baseline whenever a loop changes an
executable or synthesizable artifact. Tests, reference outputs, formal checks,
type or interface checks, synthesis constraints, numerical tolerances, or
expert review must be able to reject it before performance claims matter.

A hardware-awareness claim is credible only to the extent that the study can
show valid actions, constraint handling, relevant feedback, calibration
evidence, and a clear authority boundary.

Each applicable capability should be judged by what the method is permitted to
change, what feedback supports that change, and where its authority ends.
@tbl-hardware-awareness-assessment applies those three criteria.

| **Capability** | **What it may change** | **Feedback needed** | **Authority limit** |
| --- | --- | --- | --- |
| Vocabulary | Terms in prompts, reports, or design notes. | Review of meaning and misuse. | Naming hardware supports no design action. |
| Constraint | Candidate fields within declared budgets or limits. | Bounds, static checks, invalid-action filters, and feasibility rules. | It may enforce declared bounds, not alter or waive them. |
| Performance model | Rankings, estimates, or parameter choices inside model support. | Calibration, sensitivity, residuals, and held-out checks. | It may rank within support, not certify realized behavior. |
| Tool/environment | Code, configs, kernels, traces, or tool-invoked candidates. | Compile, run, profile, simulate, synthesize, and log tool outcomes. | A tool result supports the tested artifact and conditions, not an untested generalization. |
| Evidence and calibration | Confidence, evidence strength, and escalation recommendations. | Multi-fidelity comparison, uncertainty, provenance, failed runs, and rejected alternatives. | It may recommend escalation, not promote weak evidence to stronger evidence. |
| Authority boundary | Recommend, explain, or escalate within stated limits. | Supporting evidence, rollback cost, risk, and review context. | Recognizing the boundary does not authorize crossing it. |

: **Evidence for hardware awareness.** A study should show whether a method can represent constraints, take valid actions, obtain feedback, report evidence, and have its output rejected by an appropriate check. {#tbl-hardware-awareness-assessment tbl-colwidths="[18,24,27,21]"}

In every row, the method may act inside its declared scope but may not certify,
waive, or commit beyond it. @tbl-hardware-awareness-tests condenses this
distinction into three checks covering legal actions, model and tool support,
and the evidence required for a decision.

| **Boundary** | **Reviewer test** | **Evidence to attach** |
| --- | --- | --- |
| Semantics and legality | Does the method preserve term meanings and reject illegal actions before scoring them? | Review notes, constraint logs, invalid-action traces, and rejected examples. |
| Model and tool support | Does it state the calibrated range, escalate outside it, and preserve successful and failed tool runs? | Calibration, held-out error, commands, versions, logs, and failed runs. |
| Evidence and authority | Can it state what the evidence supports and stop before an unauthorized transition? | Evidence-source and scope labels, proxy mismatches, escalation threshold, residual risk, and decision record. |

: **A hardware-awareness claim should be falsifiable:** Reviewers should ask for
observable tests, not only fluent hardware language. {#tbl-hardware-awareness-tests tbl-colwidths="[20,38,32]"}

Calling a generator "hardware aware" does not establish what it can safely do.
A kernel generator that can compile, test, and profile has demonstrated a tool
capability. Reviewers still need its correctness tests, portability results,
calibration data, failed cases, and the decision for which those results were
used.

```{=latex}
\FloatBarrier
```

## Generation: Proposing Candidates and Artifacts

With hardware awareness established, we can examine specific method roles. Generation is the most prominent AI role because fluent artifacts are easy to
demonstrate and easy to overtrust. A method can draft an architecture
description, propose a simulator
configuration, generate code, produce a test bench, write a design-review
summary, suggest a memory hierarchy, sketch an accelerator interface, or
enumerate hypotheses about a workload. In the executed study,
generation proposed three legal array geometries, qualitative behavior, a
failure mode, and a directional mechanism with a falsifier. Schema and semantic
checks made the response a valid proposal within the declared
constraints rather than a design decision.

The danger is to mistake candidate generation for design. Generated artifacts
are useful when they expand the set of possibilities, identify alternatives, or
accelerate tedious translation. They are not credible merely because they are
well formed. A generated RTL fragment that is syntactically valid might still
fail to route or close timing during physical design. The loop still needs
validity checks, tool execution, evidence, rejected alternatives, and a named decision owner.

Candidate generation also extends beyond writing direct code. For spatial
architectures such as tensor accelerators, the AI loop must represent and
manipulate *dataflow*, the spatial and temporal unrolling of operations, as a
primary structural contract rather than a scalar hyperparameter. That choice
changes reuse, memory traffic, interconnect demand, and which tool feedback can
reject the proposal, so it also supplies a mechanism the study can test.

::: {.callout-failure-mode title="Treating AI generation as design"}
It is tempting to treat a plausible AI-generated artifact, such as an RTL fragment,
a configuration, or a benchmark question, as a result. Such an artifact is a
proposal. It may support a defensible study result only after the
AI-assisted loop tests it, records the cost and limits of the resulting evidence,
and compares it against a baseline. An architect then decides whether that
result supports a commitment. AI generation that is not embedded in a loop that
can reject it produces an unreviewed proposal, not a defensible architecture
result.
:::

The strongest near-term use of generation may be breadth. It can propose
candidate decompositions, list assumptions, create alternative experiment
plans, translate design intent into structured records, or draft the first
version of a design-loop card (@sec-appendix-b-design-loop-card gives the full card and rubric). Those outputs are valuable because they give
the architect more structured material to inspect. They become dangerous only
when the loop treats them as decisions.

Kernel generation is a useful concrete case because it isolates the software
and code-generation facet of hardware/software co-design. A compute subsystem
is viable only if its kernels, libraries, runtime paths, and target-specific
code can be generated, checked, and maintained. KernelBench, an evaluation framework for AI-generated code, asks whether language
models can generate correct and efficient GPU kernels for PyTorch workloads
[@OuyangEtAl2025KernelBench]. Its one-shot results provide a measured answer.
Each prompted task receives a candidate attempt, far fewer
candidates are correct (never more than about two-thirds of tasks, even for the
best model at the easiest level), and only a small fraction, peaking near one
task in three, run faster than the baseline they were asked to beat
(@fig-ch06-kernel-funnel). KernelBench shows that generated code is useful only
when a harness compiles it, checks numerical correctness, measures it against a
baseline, and retains failures. Follow-on kernel-generation benchmarks add
portability and backend constraints. Multi-platform settings reveal backend and portability
contracts [@WenEtAl2025MultiKernelBench], while category-aware analyses
show that correctness, task structure, numerical contracts, and efficiency can
diverge [@WangEtAl2026KernelBenchX]. As candidate generation becomes faster,
the capacity to test and reject candidates becomes the practical bottleneck.
When proposals already arrive faster than they can be
rejected (@sec-design-loop-no-longer-scales), generating more of them only
lengthens the backlog unless rejection capacity also improves.

```{python}
#| label: fig-ch06-kernel-funnel
#| fig-cap: |
#|   **Attempted is not correct, and correct is not fast:** One-shot KernelBench results for four language models across the benchmark's three difficulty levels, measured on an NVIDIA L40S GPU [@OuyangEtAl2025KernelBench]. The dashed 100 percent line marks tasks attempted, not measured fluency or plausibility. The harness's correctness oracle passes far fewer candidates, and only a small fraction are both correct and faster than the PyTorch Eager baseline. The paper prints no per-model compile rates, and these panels chart none of them; in the paper's separate 100-kernel error-analysis sample, 83 of 100 generated kernels compile and 73 execute before the correctness oracle prunes the set to 50. Values transcribed from the paper's published single-attempt tables.
#| out-width: "92%"
#| fig-alt: "Three-panel grouped bar chart of one-shot KernelBench outcomes for Llama-3.1-70B, DeepSeek V3, OpenAI o1, and DeepSeek R1 at difficulty levels one, two, and three. In each panel a dashed line at one hundred percent marks tasks attempted; blue bars show the share passing the correctness oracle, from zero to sixty-seven percent, and green bars show the share both correct and faster than the PyTorch Eager baseline, from zero to thirty-six percent. Both bars sit far below the dashed line everywhere, and the fast share is always well below the correct share."

import matplotlib.pyplot as plt
from _python.arch2_plots import COLORS, apply_style

apply_style()

# Source receipt: data/source-receipts/chapter6-kernelbench-funnel.csv
# KernelBench [@OuyangEtAl2025KernelBench], single greedy attempt on NVIDIA
# L40S: one-shot correctness (fast_0, Tables 8 and 9) and correct-and-faster-
# than-PyTorch-Eager (fast_1, Tables 1 and 9), percent of tasks per level.
# Per-model compile/execution rates are not printed in the paper (its Figure 3
# is unlabeled), so no compile-stage bars; the source record includes the paper's
# 100-kernel supplementary sample (83 compile / 73 execute / 50 correct) as
# context only.
models = ["Llama\n3.1-70B", "DeepSeek\nV3", "OpenAI\no1", "DeepSeek\nR1"]
correct = {1: [26, 43, 55, 67], 2: [0, 6, 56, 62], 3: [0, 30, 56, 8]}
fast1 = {1: [3, 6, 10, 12], 2: [0, 4, 24, 36], 3: [0, 8, 12, 2]}

fig, axes = plt.subplots(1, 3, figsize=(5.15, 1.95), sharey=True)
fig.subplots_adjust(left=0.09, right=0.985, top=0.80, bottom=0.24, wspace=0.10)

width = 0.36
xs = range(len(models))
for ax, level in zip(axes, (1, 2, 3)):
    ax.axhline(100, color=COLORS["muted"], linewidth=0.8, linestyle=(0, (4, 3)),
               zorder=1)
    ax.bar([x - width / 2 for x in xs], correct[level], width=width * 0.94,
           color=COLORS["blue"], zorder=2,
           label="correct" if level == 1 else None)
    ax.bar([x + width / 2 for x in xs], fast1[level], width=width * 0.94,
           color=COLORS["evidence"], zorder=2,
           label="correct and faster\nthan baseline" if level == 1 else None)
    ax.set_title(f"Level {level}", fontsize=6.6, pad=3)
    ax.set_xticks(list(xs))
    ax.set_xticklabels(models, fontsize=5.1)
    ax.set_ylim(0, 112)
    ax.tick_params(axis="both", labelsize=6, length=2.5, width=0.6, pad=2)
    ax.grid(axis="y", color=COLORS["grid"], linewidth=0.55)
    for spine in ["top", "right"]:
        ax.spines[spine].set_visible(False)
    ax.spines["left"].set_color(COLORS["ink"])
    ax.spines["bottom"].set_color(COLORS["ink"])
axes[0].set_ylabel("share of tasks (%)", fontsize=6.4)
axes[0].annotate("task attempted", (0.05, 100), textcoords="offset points",
                 xytext=(0, 3), fontsize=5.4, color=COLORS["muted"])
fig.legend(loc="upper center", ncol=2, fontsize=6, frameon=False,
           bbox_to_anchor=(0.5, 1.0), handlelength=1.0, columnspacing=1.4)
```
A reusable KernelBench-style evaluation records the target platform,
correctness oracle, numerical tolerance, baseline, failed kernels, portability
limits, and the rejection rule for fast but incorrect code.

The same pattern appears closer to hardware. Chip-Chat, an interactive hardware design study, reports a
conversational loop in which a language model drafts Verilog, open-source
simulation and synthesis tools check it, the resulting errors are fed back for
revision, and a small processor design proceeds as far as fabrication
[@BlockloveEtAl2023ChipChat; @ThakurEtAl2023VeriGen; @HeEtAl2024LLM4EDA]. The
generated Verilog mattered because a parser, a simulator, and a synthesis flow
could each reject a draft, while a human decided when a candidate was good
enough to commit. Those checks made the resulting decision reviewable by
another engineer.

## Prediction: Estimating Behavior Before Full Evaluation {#sec-prediction-estimating-behavior-before-full-evaluation}

While generation supplies candidate breadth, prediction is central because architecture feedback is expensive. Long before
recent foundation models[^fn-ml-foundation-models], architects used statistical and machine-learning
models to reduce the cost of exploring large design spaces. Regression models
for microarchitectural performance and power, and predictive modeling for
large architectural design spaces, are part of this lineage
[@LeeBrooks2006RegressionModeling; @IpekEtAl2006PredictiveDSE].
Architecture 2.0 uses that lineage only when the predictor states its support
region, uncertainty, calibration source, escalation trigger, and decision
owner.

[^fn-ml-foundation-models]: Recent foundation models changed expectations about prediction, but architecture's regression models and learned surrogates for design-space exploration predate them; the loop should preserve those calibration practices.

The prediction role is not limited to performance. A predictor might estimate
energy, area, latency, reliability, queueing behavior, memory traffic,
thermal behavior, compile time, implementation feasibility, or deployment
impact. It might be a regression model, a learned surrogate, a calibrated
analytic model, a simulator-backed approximation, or a hybrid that combines
domain structure with data. Predicting physical metrics (like power or latency)
without accounting for logic synthesis, place-and-route congestion, or timing
closure often leads to severe proxy mismatches. For example, a latency predictor might confidently score a proposed accelerator matrix unit, but without place-and-route feedback, it cannot see that the required SRAM banking will break timing or cause congestion. Therefore, in an agentic loop,
a predictor is not an oracle. It supplies a quantitative estimate that a
declared policy may use to prune, defer, escalate, or request stronger feedback.
The prediction capability does not set that policy or authorize the resulting
commitment.
Some of these targets are harder to pin down than others. A single-kernel
latency can be a fairly clean estimate, while deployment impact compounds power,
thermal behavior, and reliability over the life of a design, so it resists any
single-point prediction.

A predictor should report the region in which it was calibrated and the
uncertainty of each estimate. The study should show whether it has seen
similar workload regions, whether it extrapolates across a new memory behavior,
vector width, or technology assumption, whether it reports confidence, whether
it is calibrated against a higher-fidelity source, and whether it preserves
enough provenance to explain why a candidate was trusted.

There is a rigorous way to make this concrete when the assumptions are
appropriate. Conformal prediction[^fn-stats-conformal-prediction] can wrap a surrogate so
that it emits calibrated prediction sets, intervals with marginal coverage at a
user-chosen probability under an exchangeability
assumption[^fn-stats-exchangeability] and a valid calibration
set/nonconformity score [@AngelopoulosBates2021Conformal]. Architecture loops
must audit those assumptions because workloads, tools, and process corners are
often not exchangeable. The guarantee is marginal, not a promise of conditional
coverage in every workload region or design-space pocket. The study should
record the interval, calibration data, and support region, then use the result
to screen candidates or request stronger evaluation. It should not treat the
interval as approval of a design. The guarantee does not require assuming any particular error
distribution for the surrogate, which is
useful for architecture surrogates with awkward error shapes. But it still
depends on the calibration assumption being meaningful; under distribution
shift, the interval is a diagnostic that should trigger scrutiny rather than a
promise that the proxy is safe (@fig-ch06-conformal-coverage).

[^fn-stats-conformal-prediction]: Conformal prediction is a statistical technique that produces prediction sets or intervals from a model, with finite-sample coverage guarantees under an exchangeability assumption. Here, a calibrated interval may be used to prune, defer, or escalate a candidate, but it does not authorize a design decision.

[^fn-stats-exchangeability]: Exchangeability is a statistical property where the joint distribution of a sequence of random variables is invariant to their permutation, a weaker assumption than being independent and identically distributed (i.i.d.). The chapter stresses it because architecture workloads, tools, and process corners are often not exchangeable, so the conformal interval must be audited rather than trusted.

```{python}
#| label: fig-ch06-conformal-coverage
#| fig-cap: |
#|   **A conformal interval is a diagnostic, not a safety promise:** Constructed illustration of a split-conformal band around a latency-like surrogate. Inside the calibrated support region, empirical coverage matches the nominal 90% level; under distribution shift the band keeps its width while coverage collapses, so the interval should trigger scrutiny and escalation rather than certify the proxy. All values are synthetic and illustrative, not measurements of any real system.
#| out-width: "92%"
#| fig-alt: "Scatter plot of synthetic latency-like data with a fixed-width conformal prediction band around a surrogate curve. In the calibrated support region on the left, nearly all points fall inside the band; in the distribution-shift region on the right, observed behavior rises away from the unchanged band and most points escape it, showing coverage collapse."

import math
import random
import matplotlib.pyplot as plt
from _python.arch2_plots import COLORS, apply_style

apply_style()

# Constructed illustration: every value is synthetic, generated in this cell
# with a fixed seed; no real system was measured. The split-conformal recipe
# (calibration residual quantile -> fixed-width band) follows the conformal
# prediction pattern cited in the text.
rng = random.Random(20260713)
SHIFT_X = 6.0
NOISE = 0.32
X_MAX = 10.0

def surrogate(x):
    # The pattern the surrogate learned inside the calibrated region.
    return 4.0 + 1.8 * math.sin(0.9 * x) + 0.25 * x

def truth(x):
    # Beyond the calibrated region the workload regime changes.
    drift = 1.1 * (x - SHIFT_X) ** 1.25 if x > SHIFT_X else 0.0
    return surrogate(x) + drift

def observe(x):
    return truth(x) + rng.gauss(0.0, NOISE)

# Split conformal: the 90% quantile of absolute calibration residuals from the
# supported region sets a fixed band half-width q.
cal_x = [rng.uniform(0.0, SHIFT_X) for _ in range(60)]
cal_scores = sorted(abs(observe(x) - surrogate(x)) for x in cal_x)
q = cal_scores[math.ceil(0.9 * (len(cal_scores) + 1)) - 1]

test_in = [(x, observe(x)) for x in [0.15 + i * (SHIFT_X - 0.3) / 24 for i in range(25)]]
test_out = [(x, observe(x)) for x in [SHIFT_X + 0.15 + i * (X_MAX - SHIFT_X - 0.3) / 24 for i in range(25)]]
cov_in = sum(abs(y - surrogate(x)) <= q for x, y in test_in) / len(test_in)
cov_out = sum(abs(y - surrogate(x)) <= q for x, y in test_out) / len(test_out)

xs = [i * X_MAX / 400 for i in range(401)]
fig, ax = plt.subplots(figsize=(4.9, 2.55))
fig.subplots_adjust(left=0.11, right=0.97, top=0.86, bottom=0.19)

ax.fill_between(xs, [surrogate(x) - q for x in xs], [surrogate(x) + q for x in xs],
                color=COLORS["blue"], alpha=0.16, linewidth=0, zorder=1)
ax.plot(xs, [surrogate(x) for x in xs], color=COLORS["blue"], linewidth=1.1, zorder=2)
ax.plot(xs, [truth(x) for x in xs], color=COLORS["muted"], linewidth=0.9,
        linestyle=(0, (4, 3)), zorder=2)

ax.scatter([x for x, _ in test_in], [y for _, y in test_in], s=9,
           color=COLORS["blue"], zorder=3)
miss = [(x, y) for x, y in test_out if abs(y - surrogate(x)) > q]
hit = [(x, y) for x, y in test_out if abs(y - surrogate(x)) <= q]
ax.scatter([x for x, _ in hit], [y for _, y in hit], s=9, color=COLORS["blue"], zorder=3)
ax.scatter([x for x, _ in miss], [y for _, y in miss], s=11, color=COLORS["red"], zorder=3)

ax.axvline(SHIFT_X, color="#AAB4BF", linewidth=0.7, linestyle="--", zorder=1)
ymin, ymax = ax.get_ylim()
ax.set_ylim(ymin, ymax + 1.2)
ax.text(SHIFT_X / 2, ax.get_ylim()[1] - 0.35, "calibrated support", ha="center",
        va="top", fontsize=6.0, fontweight="bold", color=COLORS["workload_ink"])
ax.text((SHIFT_X + X_MAX) / 2, ax.get_ylim()[1] - 0.35, "distribution shift",
        ha="center", va="top", fontsize=6.0, fontweight="bold", color=COLORS["constraints_ink"])
ax.text(SHIFT_X / 2, ax.get_ylim()[1] - 1.15,
        f"empirical coverage ≈ {cov_in:.0%}\n(nominal 90%)", ha="center", va="top",
        fontsize=5.4, color=COLORS["workload_ink"])
ax.text((SHIFT_X + X_MAX) / 2, ax.get_ylim()[1] - 1.15,
        f"same band width,\ncoverage ≈ {cov_out:.0%}", ha="center", va="top",
        fontsize=5.4, color=COLORS["constraints_ink"])

ax.annotate("surrogate ± conformal band", (1.7, surrogate(1.7) + q),
            textcoords="offset points", xytext=(-2, 5), fontsize=5.6,
            color=COLORS["workload_ink"])
ax.annotate("observed behavior", (8.2, truth(8.2)), textcoords="offset points",
            xytext=(-58, 2), fontsize=5.6, color=COLORS["muted"])

ax.set_xlabel("workload / design parameter (synthetic units)", fontsize=7)
ax.set_ylabel("latency-like metric\n(synthetic units)", fontsize=7)
ax.set_xlim(0, X_MAX)
ax.tick_params(axis="both", labelsize=6.5, length=2.5, width=0.6, pad=2)
ax.grid(axis="y", color=COLORS["grid"], linewidth=0.6)
for spine in ["top", "right"]:
    ax.spines[spine].set_visible(False)
ax.spines["left"].set_color(COLORS["ink"])
ax.spines["bottom"].set_color(COLORS["ink"])

plt.show()
plt.close(fig)
```

Nothing in the surrogate's own report changes at the shift boundary in
@fig-ch06-conformal-coverage. The band keeps its width and its nominal 90
percent claim; only the observed points climbing out of it reveal that
empirical coverage has collapsed. When observations begin to fall outside the
interval, the study should stop relying on the surrogate and request a
higher-fidelity evaluation.

The executed study recorded this distinction. The model's qualitative
prediction and mechanism helped select a test; neither counted as performance
evidence. SCALE-Sim evaluated the candidates, and the matched probe falsified
the mechanism. A rough predictor may still be enough to discard obviously bad
candidates, but consequential claims require calibration and independent
observations matched to the decision.

## Optimization: Learning the Design Space

Once candidates are generated and their behavior predicted, the loop must decide what to evaluate next. Optimization is often framed as search, finding the best point under an
objective. Architecture needs a richer formulation. Unlike the continuous, differentiable weight spaces of neural network training, hardware design spaces are often discrete, non-differentiable, and highly constrained, so the methods that matter are the ones built for sparse, expensive feedback and hard validity limits. The useful goal is to
learn the design space well enough to make a defensible decision under
limited feedback. That may mean finding a Pareto region[^fn-math-pareto-region], identifying a
constraint boundary, understanding a sensitivity, ruling out a class of
candidates, or deciding which expensive experiment is worth running next.

[^fn-math-pareto-region]: A Pareto region here is the Pareto frontier introduced earlier, treated as one defensible target for architecture optimization under limited feedback, alongside a constraint boundary or a ruled-out class of candidates, rather than collapsing the search to a single best point.

Bayesian optimization was developed for sequential experiments on expensive
black-box functions [@JonesSchonlauWelch1998EGO;
@SnoekLarochelleAdams2012BayesianOptimization]. Its surrogate and acquisition
rule balance exploration against exploitation and make uncertainty part of the
choice of what to evaluate next. That structure fits architecture settings in
which simulation, synthesis, or measurement runs are costly. When the study
records the acquisition rule, score, and current uncertainty, a reviewer can
also examine why a particular candidate received the next evaluation.
For each choice, the study should record the candidate, evaluation fidelity,
expected benefit, and stopping condition. The optimizer selects experiments;
it does not make the final architecture decision.

Reinforcement learning is attractive for sequential problems such as placement
decisions, scheduling policies, adaptive control, or multi-stage design flows.
This does not make every architecture task a reinforcement-learning problem.
Method choice depends on state, action, transition,
feedback, and the decision the evidence must support.

A narrower example shows what happens when the design space is defined tightly
enough that the environment can supply clearer rejection authority. PrefixRL, a hardware-aware RL framework, casts the design of parallel-prefix
arithmetic circuits, such as adders, as a reinforcement-learning problem with
logic synthesis in the loop, so every proposed circuit is scored by an actual
synthesis run rather than a hand-built proxy [@RoyEtAl2021PrefixRL]. The
relevant result here is narrower than a leaderboard. Each candidate came from
the declared circuit space, was evaluated by a synthesis-backed environment,
and was advanced or rejected by scoped evidence stronger than a hand-built
proxy.
The same method family is harder to interpret when its reward is a fast proxy
and easier to interpret when a restricted action space lets a stronger instrument
reject each candidate. Method choice is inseparable from what the environment
can verify.

::: {.callout-war-story title="A classical baseline remained competitive"}
**The claim.** AlphaChip drew attention because reinforcement learning was applied to macro
placement [@MirhoseiniEtAl2021GraphPlacement].

**The gap.** In a later public assessment, a strong simulated-annealing baseline matched or beat the learned method on its
proxy cost for most modern test cases. The original authors disputed whether
that assessment reproduced the method and its intended setting
[@ChengEtAl2023AssessmentRL; @GoldieEtAl2024ThatChipHasSailed].

**The lesson.** This does not establish that reinforcement learning is the wrong method for
placement. It shows that added method complexity does not establish an
advantage by itself. A learned method earns the stronger claim only when a
shared evaluation shows what it improves over a well-tuned simpler baseline.
:::

The executed study in @sec-running-the-loop reaches a narrower version of the
same conclusion. One model proposal competes with a deterministic aspect-ratio
heuristic under equal four-evaluation budgets. Both arms retain the same
baseline, and the held-out probe falsifies the model's mechanism account. The
architecture outcome, AI contribution, and explanation are evaluated
separately, so the study does not claim an AI advantage that it did not observe.

The same baseline requirement appears in autotuning and compiler optimization.
AlphaDev's discovered sorting routines entered production only after passing
the same correctness harness applied to human-written code
[@MankowitzEtAl2023AlphaDev], and AutoPhase's high-level synthesis phase
orderings are scored by actual HLS runs [@HajAliEtAl2020AutoPhase]. These
results depend on correctness tests and actual tool runs, not model output
alone. General
program-autotuning frameworks such as OpenTuner [@AnselEtAl2014OpenTuner], and
tensor-program optimizers such as AutoTVM and Ansor, share a loop structure
worth naming. A learned cost model proposes promising candidates, the loop
compiles and measures the strongest of them on real hardware, and those
measurements correct the cost model for the next round
[@ChenEtAl2018AutoTVM; @ZhengEtAl2020Ansor]. The cheap proxy is never trusted
alone, because measured hardware feedback continually re-grounds it. These
systems provide useful boundary cases for this discussion because they exercise
the compiler/runtime path through legal schedules, measured
hardware feedback, cost-model updates, failed variants, and a clear limit on
what those measurements prove. Floorplanning stresses high-consequence physical
feedback; autotuning stresses the compiler/runtime path that makes
specialization usable. A comparable study should retain the candidate
schedule, compiler/runtime version, measurement context, failed variants,
cost-model update, and the declared limit on what the measurement can prove.

The optimizer should therefore be evaluated by what it learns about the design
space, how it spends feedback, and what it can explain, not only by its best
score. Five questions make that evaluation concrete:

- Did it discover a robust region?
- Did it identify a proxy mismatch?
- Did it spend high-fidelity evaluations where they changed the decision?
- Did it preserve rejected alternatives?
- Did it show why one candidate was chosen over another?

If the study cannot answer these questions, the loop may have optimized a
number without improving architectural understanding.

## Sample Efficiency Under Expensive Feedback {#sec-sample-efficiency-under-expensive-feedback}

Sample efficiency measures how much decision-relevant information a method
obtains from each costly evaluation.[^fn-ml-sample-efficiency] This matters in
architecture because high-fidelity simulator, synthesis, and hardware runs are
scarce.
Many AI settings assume abundant feedback. Architecture often has the
opposite shape, with a small number of high-fidelity runs, a larger number of
medium-fidelity simulations, many cheap proxies, and a long tail of hidden
costs such as expert review, tool setup, debugging, and license availability.

[^fn-ml-sample-efficiency]: In this chapter, sample efficiency is the amount of decision-relevant evidence a loop gains per scarce feedback event, including failed runs and expert rejections alongside successful simulations. Framing it this way makes it a method-selection criterion, since the loop should spend feedback where decision value per unit cost is highest rather than maximize the raw number of runs.

Architecture design spaces hold orders of magnitude more candidates than any
real sample budget can reach. That gap runs across a logarithmic count scale in
@fig-evidence-gap. The upper
rows reuse the design-space anchors from @sec-design-loop-no-longer-scales,
12,960-state slice computed there, the accelerator DSE scale of MAESTRO,
AutoTVM-style operator tuning spaces, and the core mapspace expression of
Timeloop [@KwonEtAl2019MAESTRO; @ChenEtAl2018AutoTVM; @ParasharEtAl2019Timeloop]. The
lower rows reuse the representative feedback budgets from @sec-architecture-environments-tool-interfaces. High-fidelity architecture evidence can touch only a tiny slice of
the plausible candidate space. Between the more than $10^{18}$ candidates in
the Timeloop mapspace and the one to five silicon or deployment checks a study
can afford, the gap spans roughly eighteen orders of magnitude, so methods
must decide what can be screened by proxies, what should be escalated, and
what rejected regions must be recorded.
The gap is why sample-efficient methods exist, not a proof that the search is
hopeless.

```{python}
#| label: fig-evidence-gap
#| fig-cap: |
#|   **Architecture methods face an evidence gap:** Design spaces can contain orders of magnitude more candidates than a loop can afford to test at stronger feedback levels. The plot compares counts only as scale intuition: source-backed or transparent design-space anchors above the divider, representative sample-budget ranges below it.
#| out-width: "100%"
#| fig-alt: "Log-scale comparison of large design-space candidate counts against much smaller affordable feedback sample budgets, highlighting an evidence gap."

import matplotlib.pyplot as plt
from _python.arch2_plots import COLORS, add_note_box, apply_style, draw_range_rows, top_log_axis

rows = [
    {"display_label": "12,960-state slice", "display_note": "before corners/workloads", "count_low": 12_960, "count_high": 12_960, "right_label": "~1.3e4", "color": COLORS["orange"]},
    {"display_label": "MAESTRO accelerator DSE", "display_note": "reported candidate search scale", "count_low": 480_000_000, "count_high": 480_000_000, "right_label": "480M", "color": COLORS["orange"]},
    {"display_label": "AutoTVM operator tuning", "display_note": "reported order-of-billions space", "count_low": 1_000_000_000, "count_high": 1_000_000_000, "right_label": "~1B", "color": COLORS["orange"]},
    {"display_label": "Timeloop mapspace core", "display_note": "lower bound before factor choices", "count_low": 2_600_000_000_000_000_000, "count_high": 2_600_000_000_000_000_000, "right_label": ">2e18", "color": COLORS["orange"]},
    {"display_label": "cheap proxy screening", "display_note": "representative affordable checks", "count_low": 100_000, "count_high": 1_000_000, "right_label": "1e5-1e6", "color": COLORS["blue"]},
    {"display_label": "cycle-level comparisons", "display_note": "representative scoped runs", "count_low": 10, "count_high": 100, "right_label": "10-100", "color": COLORS["blue"]},
    {"display_label": "RTL / physical feedback", "display_note": "scarce high-fidelity samples", "count_low": 3, "count_high": 20, "right_label": "3-20", "color": COLORS["blue"]},
    {"display_label": "silicon / deployment checks", "display_note": "few high-consequence samples", "count_low": 1, "count_high": 5, "right_label": "1-5", "color": COLORS["blue"]},
]

apply_style()
fig, ax = plt.subplots(figsize=(5.35, 2.95))
fig.subplots_adjust(left=0.38, right=0.85, top=0.82, bottom=0.27)

y_positions = [0.35, 1.35, 2.35, 3.35, 5.80, 6.80, 7.80, 8.80]
ax.set_ylim(-1.05, y_positions[-1] + 0.65)
ax.invert_yaxis()
ax.set_yticks([])
top_log_axis(
    ax,
    xlim=(0.8, 1e19),
    xticks=[1, 1e2, 1e4, 1e6, 1e9, 1e12, 1e15, 1e18],
    xticklabels=["1", r"$10^2$", r"$10^4$", r"$10^6$", r"$10^9$", r"$10^{12}$", r"$10^{15}$", r"$10^{18}$"],
    xlabel="count scale: candidates or affordable samples",
    tick_fontsize=6.0,
)

ax.axhline(4.45, color=COLORS["muted"], linewidth=0.65, linestyle="--", zorder=0)
ax.text(-0.74, -0.55, "design-space anchors", transform=ax.get_yaxis_transform(), ha="left", va="center", fontsize=5.8, fontweight="bold", color=COLORS["methods_ink"], clip_on=False)
ax.text(-0.74, 5.10, "affordable feedback budgets", transform=ax.get_yaxis_transform(), ha="left", va="center", fontsize=5.8, fontweight="bold", color=COLORS["note_text"], clip_on=False)

draw_range_rows(ax, rows, low_key="count_low", high_key="count_high", label_x=-0.68, right_x=1.04, label_fontsize=6.15, right_fontsize=5.85, y_positions=y_positions, show_notes=False)
add_note_box(fig, "High-fidelity evidence samples a tiny slice\nof the plausible candidate space.", xywh=(0.12, 0.035, 0.76, 0.10), fontsize=5.4)

plt.show()
plt.close(fig)
```

The cost records described in @sec-data-representations-world-models can guide the next experiment. A useful sample
is a simulator result, warning, failed run, synthesis report, measurement, or
expert review that reduces uncertainty relevant to the decision. The study
should not maximize sample count. It should request feedback where decision
value per unit cost is highest:
$$
V_{\mathrm{sample}} \approx \frac{\Delta D}{C_{\mathrm{sample}}},
$$
where $\Delta D$ denotes the change in decision confidence, rejected-space
coverage, or evidence strength produced by that sample. Neither term is
directly measurable; the relation is a way to rank candidate experiments by
what they would resolve, not a number to compute and literally optimize.

The cost term includes the AI method itself. Large-model inference, agent
fanout, retrieval, fine-tuning, GPU-hours, energy, carbon exposure, and the
human time spent reviewing model output are part of $C_{\mathrm{sample}}$ when
they are needed to obtain the feedback. Treating generation as free simply moves
cost outside the supporting evidence record and can make an already expensive loop look cheaper than
it is. When energy or carbon exposure is material, that record should also state
the location, time basis, energy source, utilization, and amortization
assumptions.

This heuristic has a formal analogue in idealized settings. Bayesian optimization makes the
choice of the next sample an explicit acquisition function (a rule that scores
each candidate point) over a surrogate's posterior (its current probability
estimate over outcomes). GP-UCB[^fn-ml-gp-ucb] (Gaussian Process Upper Confidence
Bound) offers a formal cumulative-regret bound[^fn-ml-no-regret] under stated
kernel, smoothness, and noise assumptions. The logged acquisition rule, not the
bound alone, lets the loop explain why it sampled where it did
[@SrinivasEtAl2010GPUCB]. Multi-fidelity Bayesian[^fn-ml-multi-fidelity-bo]
optimization goes one step further and treats the fidelity level itself as a
decision variable. It chooses not only which candidate to evaluate but whether
to spend a cheap proxy or an expensive simulation on it, exactly the choice the
feedback budget poses [@KandasamyEtAl2017MultiFidelityBO]. In practice, the
study should record the acquisition rule, fidelity choice, assumptions,
returned result, rejected alternatives, and escalation threshold.

[^fn-ml-gp-ucb]: GP-UCB (Gaussian Process Upper Confidence Bound) is an acquisition function that selects the next point to evaluate by weighing both the expected reward and the uncertainty of a Gaussian Process model. Its value here is that the acquisition rule is auditable rather than ad hoc. The loop can record why uncertainty made a sample worth spending, then check the assumptions separately.

[^fn-ml-multi-fidelity-bo]: Multi-fidelity Bayesian optimization jointly chooses which design to evaluate and at which fidelity level, trading information gain against cost. That is the feedback-budget decision here, whether to spend a cheap proxy or an expensive simulation on a candidate. @sec-feedback-verification-trust explains what the different feedback levels can establish.

[^fn-ml-no-regret]: In this chapter, a no-regret guarantee is a formal promise about a sampling rule under stated assumptions, such as a suitable kernel, bounded noise, and smoothness of the objective. It is useful only when the loop records whether those assumptions actually apply. Under those assumptions, the guarantee bounds cumulative regret across the sampling sequence; the logged acquisition score explains a particular choice.

The feedback budget falls into a few operational regimes, laid out in
@tbl-sample-efficiency-regimes with illustrative numbers rather than
prescriptions. Method choice must change when feedback is measured in milliseconds,
minutes, hours, weeks, or silicon cycles.
Cheap evaluations allow broad screening, while expensive tools provide
observations that cheaper models cannot. Cost and fidelity alone do not
determine whether a result is sufficient for a decision. Methods that ignore this distinction
will either exhaust the budget or produce unverified claims.
@sec-feedback-verification-trust develops the broader hierarchy of feedback
levels and asks what each can establish.

| **Regime** | **Typical setting** | **Method implication** | **Evidence requirement** |
| --- | --- | --- | --- |
| Many cheap proxy runs | Analytic models, rough estimators, compiler hints. | Broad search, candidate generation, surrogate pretraining. | Track proxy validity and avoid overfitting the cheap metric. |
| Hundreds of simulations | Simulator-backed DSE or workload sweeps. | Bayesian optimization, active learning, transfer, sensitivity analysis. | Record seeds, configs, workloads, and failed runs. |
| Tens of expensive tool runs | Synthesis, physical design, emulation, or hardware-in-the-loop. | Strong priors, staged screening checks, human filtering, small candidate sets. | Require calibration and named rejection authority. |
| Few high-consequence checks | Silicon, deployment, fleet experiments, or customer workloads. | Critique, evidence organization, conservative recommendations. | A named decision owner and audit trail dominate. |

: **Feedback regime should drive method choice:** Cheap proxies, moderate simulations, expensive EDA, and scarce high-consequence checks reward different mixtures of generation, prediction, optimization, critique, and verification. {#tbl-sample-efficiency-regimes tbl-colwidths="[18,24,26,22]"}

This is where records of failure matter. Failed simulations, invalid
candidates, timeouts, rejected configurations, and proxy mismatches are not noise to be
discarded. They are information about the boundary of the design space. A
sample-efficient loop should learn from what failed, not only from the points
that produced clean plots.

Sample efficiency also depends on representation. If the environment logs
only final scores, the method cannot reuse much. If it records workload
metadata, candidate structure, tool warnings, failure reasons, and fidelity
level, then each sample contributes more. @sec-data-representations-world-models and @sec-architecture-environments-tool-interfaces are therefore
not preliminaries to methods. They determine whether methods can learn.

## Critique, Repair, and Explanation

Beyond generating, predicting, and optimizing, loops often need to evaluate existing artifacts rather than propose new ones. Critique may be the most underrated method role in Architecture 2.0. Many
loops do not need a generative method to invent a new design. They need a system that can
read a proposed design, identify missing assumptions, check whether evidence
matches claims, compare alternatives, find invalid actions, repair artifacts,
or explain a tradeoff for review.

This role is especially attractive because it can operate against existing
human artifacts. A critic can inspect a design-space report, simulator log,
configuration file, benchmark description, or paper draft. It can ask whether
the workload matches the claim, whether the metric is a proxy for the real
objective, whether rejected candidates are missing, whether tool versions are
recorded, or whether a table proves less than the prose claims.

Question-answering resources such as QuArch point toward one piece of this
problem, making architecture knowledge accessible to automated optimizers and reviewers
[@PrakashEtAl2025QuArch]. But critique requires more than answering
questions from papers. It needs the loop state that papers often omit:
assumptions, tool settings, failed runs, rejected alternatives, supporting
evidence, and the decisions made from it.

Critique can therefore increase throughput as well as improve review.
In a rejection-bound loop, a critic that catches a missing workload record,
unsupported proxy claim, stale tool version, or invalid action before an
expensive run can improve the loop more than another generator. It raises the
rate at which weak claims are rejected safely. Adjacent code-review evidence
shows both the opportunity and the boundary. In one RLHF code setting,
model-written critiques caught more inserted bugs than the study's contractor
pool, while human-plus-critic teams wrote more comprehensive critiques than
contractors alone and hallucinated fewer problems than models alone
[@McAleeseEtAl2024CriticGPT]. That result motivates an assisted architecture
review role; it is not evidence that the same gain transfers without testing.
ML-assisted resolution of code-review comments runs at production scale at
Google [@FroemmgenEtAl2024CodeReview].

Repair methods can fix malformed configurations, syntax errors, test benches,
or reports. A method can propose a corrected
configuration, fix a malformed constraint or tool-syntax error, patch a test
bench, regenerate a plot with the right workload metadata, or produce a clearer
design-loop card. They cannot waive a timing, power, interface, or signoff
failure that the design actually violates. The explanation should tell the
architect what failed, what evidence is missing, and why further evaluation may
be worthwhile.

::: {.callout-architect-checkpoint title="Constraint Waivers"}
An automated method may repair malformed artifacts, but the repair role does
not include authority to relax a design constraint. The loop must explain the
violation and obtain a recorded waiver from the named authority before
proceeding.
:::

Verification, the remaining checking role, is developed separately in
@sec-feedback-verification-trust.
@sec-feedback-verification-trust develops it as the family of rejection
authorities a loop needs, not as one more method to pick.

## Choosing a Method Under Constraints {#sec-choosing-a-method-under-constraints}

With these distinct roles defined, a good method choice should survive a design
review. The reviewer needs to know the task the method serves, the
representation it reads and writes, the environment in which it acts, the
feedback it can afford, the evidence that would support its output, what can
reject it, and what happens if it is wrong.

The decision matrix in @tbl-method-selection-matrix does not produce an
automatic answer. Each question forces the architect to confront the structural
realities of the specific loop.

| **Question** | **If the answer is favorable** | **If the answer is unfavorable** |
| --- | --- | --- |
| Are the task and authority limits stated? | Use stronger automation inside those limits. | First decompose the task or keep the method advisory. |
| Does the loop need more candidates? | Use generation for breadth, with validity checks attached. | Prefer critique, verification, evidence organization, or better rejection tests. |
| Are actions validatable? | Let the environment reject illegal candidates. | Use generation only with strict human/tool review. |
| Is feedback cheap enough? | Search, active learning, or online adaptation may be useful. | Use priors, surrogates, staged screening checks, and critique. |
| Is uncertainty reported? | Prediction can guide exploration. | Avoid treating point estimates as evidence. |
| Is the commitment reversible? | Higher autonomy may be acceptable. | Require stronger evidence and a named decision owner. |
| Is provenance recorded? | Claims can be traced and audited; a replay package can additionally support rerunning the work. | Do not make strong comparative claims. |

: **Method selection follows loop conditions:** The useful mix of method roles depends on action validity, feedback cost, fidelity, rollback cost, and rejection authority. {#tbl-method-selection-matrix tbl-colwidths="[20,31,31]"}

Favorable answers expand what a method can do inside the loop. They do not, by
themselves, expand what the loop may commit.

The matrix also clarifies why the same method may be appropriate in one
architecture loop and inappropriate in another. A generator may be acceptable
for drafting candidate simulator configs but not for committing a physical
design change. A surrogate may be useful for ranking early candidates but not
for final power claims. An RL policy may be reasonable in a reversible
runtime-control loop but not in a high-consequence design decision without
strong rejection authority.

::: {.callout-note title="Propose, test, and stop"}
The executed study assigned the model proposal rights, not simulation,
rejection, recommendation, or commitment rights. A frozen heuristic supplied a
fair comparison arm, SCALE-Sim supplied the domain observations, and a matched
probe challenged the proposed mechanism. When neither arm cleared the declared
margin and the mechanism failed, the study stopped with a tie and a falsified
explanation. The method roles made that negative result usable.
:::

A paper should explain its method choice in ordinary terms, including the task,
permitted actions, evaluation budget, required evidence, and rejection checks.

The same rule covers multi-participant implementations. Splitting work across a
generator, a predictor, a critic, a verifier, an explainer that documents the
evidence record, and a coordinator that routes state and work may
improve throughput, specialization, or coverage, but it does not itself
strengthen the claim. If several components share the work, the paper should
state what each component reads and writes and who reviews its output. Adding
more agents without these records makes the study harder to audit.

::: {#pri-match-methods .callout-design-principle title="Choose the method for the job"}
Start with the job the study needs done, then choose a method suited to it.
Generating, predicting, optimizing, critiquing, repairing, verifying,
explaining, and coordinating impose different validation demands. Evidence
that a method performs one job does not authorize it to perform another.
:::

The open questions concern how to enforce those role boundaries and allocate
scarce feedback among them.

## Open Research Questions

The research problem is whether a method performs its assigned role inside one
architecture study with stated evidence limits. A first project should record
that role's action, feedback, rejection, and decision boundary.

1. **What role contracts keep AI methods inside assigned authority and govern handoffs?**
The role-limit table in @tbl-method-role-limits assigns generators,
predictors, critics, and optimizers to different obligations, while the
method-selection matrix in @sec-choosing-a-method-under-constraints tests the
conditions for choosing among them. Runtime enforcement remains weak. The
useful artifact is a role-contract language plus a monitor
that detects when a method silently escalates its authority, such as a predictor
turning a scoped estimate into an unsupported decision. The contract should
also govern handoffs when feedback cost, uncertainty, or consequence changes.
Public loop traces or scoped test environments should measure violations,
false alarms, and the cost of preserving the decision boundary.

2. **How should loops spend scarce feedback across method roles?**
Generation, prediction, critique, repair, and optimization all consume
feedback, but they do not consume it with the same value. The core problem is a
feedback-allocation model across method roles under simulator-hour, validation,
latency, and human-review budgets. The model should show when a loop should
generate another candidate, refine a predictor, ask a critic to search for
missing evidence, or escalate to stronger feedback. The decision-value framing
in @sec-sample-efficiency-under-expensive-feedback supplies the budget model.

3. **How can optimizers learn from failures without losing fidelity labels?**
Sample efficiency under expensive feedback depends on learning from a mixture
of cheap warnings, proxy mismatches, synthesis timeouts, and rare high-fidelity
failures. A thesis-level paper would introduce acquisition functions or
training objectives that ingest heterogeneous failure records while preserving
their fidelity labels. The result should be tested on a design-space
exploration task where the method improves sample efficiency without allowing
cheap negative evidence to masquerade as high-fidelity rejection.

4. **What rejection suites test hardware-aware generators?**
The architecture community lacks standardized adversarial tests designed to
falsify unsupported predictions or trap invalid generative actions. The right
target is not a definitive oracle for all hardware awareness. A rejection suite
for a named environment should state the maximum commitment that
passing the suite can authorize. It should include intentionally invalid
candidates, near-miss candidates, and valid candidates, so false-pass and
false-reject behavior can be measured.

## Conclusion

This chapter asked which role an AI method should play and what feedback would
make its output credible. Method selection should begin with the work the study
needs done. Generation, prediction, optimization, critique, repair,
verification, explanation, and coordination require different feedback and
support different claims. Capability within one of those roles does not
authorize a method to widen its scope, waive a constraint, or commit the
architecture.

Because high-fidelity evaluation is expensive, a study should use cheap checks
to screen candidates and reserve stronger tools for questions that affect the
decision. Model rankings will change, but the need to state each method's job,
evidence, and authority will remain. The best methods help architects learn the
design space instead of only searching it harder.

::: {.callout-carry-forward}
- **Carry forward:** Method choice is a role-and-budget decision, not a leaderboard. Capability inside a role does not grant broader authority.
- **Reader test:** For a given loop, can you name each method's role, feedback budget, rejection path, and authority limit?
- **Up next:** Once models and methods can act, the next question is whether their feedback becomes evidence adequate for the proposed transition.
:::
