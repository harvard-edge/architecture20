# Chapter 5 Figure Plan

Current figures:

- `F6-environment-tool-interface`: explicit environment contract.

Executable Quarto figures in the chapter:

- `fig-fidelity-cost-confidence`: Matplotlib feedback fidelity and cost plot.

Needed next:

- Consider a small interface-contract example that maps ISA, compiler/runtime,
  simulator, and benchmark harness obligations to valid actions.
- Keep diagrams rectangular and contract-like; this chapter should feel like
  architecture interface design, not workflow automation.
- Any future tool figure should name feedback type, latency, fidelity, and
  rejection path.
