# Bootstrapping a Bounded Architecture Study {#sec-appendix-a-bootstrapping}

```{=latex}
\abstract*{This appendix gives a practical path for starting a small, credible AI-assisted architecture study without turning the material into a tool manual. The first study should be small enough for another architect to inspect, open to contrary evidence, and tied to a decision with a named owner.}
```

The smallest useful starting point is not "install an AI assistant" or
"automate the design loop." It is a bounded architecture study with a question,
baseline, alternatives, representation, tool path, method role, evidence
standard, explanation test, and decision owner. When the work iterates,
a design loop executes and revises parts of that study. The loop is a process
inside the study, not the whole of Architecture 2.0.

> **Bootstrap study.** A bootstrap study is the smallest credible unit of
> AI-assisted architecture work. It bounds one architecture question and its
> alternatives, connects them to a tool path another architect can inspect, and produces evidence
> and a decision record that another architect can challenge.

This appendix is self-contained and restates context developed more fully in
the main text. A large automated research harness may eventually include many
tools, critics, planners, datasets, dashboards, and review steps. Begin instead
with a bounded study that produces a review record another architect can read.
For a student or new researcher, a finished bootstrap study is useful when that
record lets another architect inspect its assumptions and challenge its result,
even if the AI role inside it is modest.

The study's execution pattern is deliberately minimal (@fig-bootstrap-loop).
The figure shows a short path from formulating the task to recording evidence,
testing an explanation, and making a bounded decision. A reviewer can return
any earlier step for revision.
Choose one task, one representation, one tool wrapper, one method
role, one set of evidence checks, and one person or team authorized to decide. If no possible result
could count against the claim, the study is not ready to support a decision.

![**A first AI-assisted architecture study should be small and challengeable.** The study may use an iterative design loop, but it should grow only after recording its question, representation, tool path, method role, evidence checks, explanation test, and decision rights.](images/F12-bootstrap-loop){#fig-bootstrap-loop width="100%" fig-alt="A bounded architecture study containing a six-step execution path from formulating a task through recording evidence, explanation, and a bounded decision, with review able to revise any earlier part of the study."}

## The Architecture Study Playbook

When incorporating AI into an architecture study or hardware/software co-design
effort, begin by defining the question, alternatives, artifact path, evidence,
explanation, and decision rather than by selecting a model.
AI then has a method role inside that work. It may generate, predict, optimize,
critique, repair, verify, explain, or coordinate, but it does not own the
architecture decision.

:::: {.callout-architect-checkpoint}
Before adding an AI role, name the architecture question and the decision it may
inform. Define the baseline, candidate space, tool feedback, evidence path,
mechanism test, evidence that could count against the result, and the person or
team authorized to act on the result or escalate it. Only then choose a narrow
AI role that reduces study cost, expands candidate coverage, shortens feedback
time, or makes the output easier to inspect without weakening the evidence
standard or exceeding its assigned authority.
::::

@tbl-silicon-playbook starts with the architecture work rather than the AI
tool. For each capability, it identifies the study record, a useful AI role,
and the check needed before relying on the output.

| **Capability** | **Study record** | **AI role that may help** | **Check before relying on it** |
|---|---|---|---|
| **Formulate** | Question, workload slice, baseline, objective, constraints, non-goals, and claim boundary. | Critic or planner that identifies missing state. | Stop if no result could count against the claim. |
| **Explore** | Meaningful alternatives, legal actions, invalid states, deferred choices, and prior rejections. | Generator or optimizer constrained to legal moves. | Reject outputs that invent actions, interfaces, or assumptions. |
| **Implement** | Checkable artifacts, tool bindings, configurations, seeds, costs, and failure records. | Tool caller, code generator, or coordinator. | Reject runs without a valid environment contract and provenance. |
| **Evaluate** | Representative workloads, matched baselines, evidence scope, uncertainty, counterevidence, and total feedback cost. | Predictor, surrogate, active learner, or verifier. | Escalate when the proposed claim or transition exceeds the evidence. |
| **Explain** | Mechanism hypothesis, directional prediction, and a sensitivity, intervention, or ablation that can distinguish it. | Critic, explanation generator, or experiment planner. | Treat a story that merely restates the outcome as untested. |
| **Defend** | Tradeoffs, rejected alternatives, limits, reversal conditions, decision rights, and their holders. | Decision support or bounded automated review. | The method cannot expand its own authority or waive a failed constraint. |

: **The architecture study playbook starts from the work, not the tool.** The six capabilities recur throughout a study rather than forming a rigid pipeline. Each row names an observable record, a legitimate AI role, and a check that keeps a plausible output from becoming an unsupported architecture decision. {#tbl-silicon-playbook .playbook-table tbl-colwidths="[13,33,25,25]"}

This is deliberately smaller than an enterprise AI strategy. It is an
architecture study playbook because it treats architecture work as coupled to
tools, costs, constraints, evidence, and difficult-to-reverse decisions. A team
can apply it to an accelerator search, a memory-hierarchy study, a
compiler/runtime option, a benchmark update, or a verification triage task. In
each case, record the study and its execution path before expanding the method's
role.

The rest of this appendix walks the execution path in @fig-bootstrap-loop. The
playbook organizes the work by learner capability, while the sections below
organize the same work by build step.

## Choose a Bounded Task

Start with a task where success and failure can be inspected. Good first
tasks include a small design-space exploration, workload characterization,
configuration search, benchmark generation, design review, or report
critique. Avoid starting with "design a processor" or "automate the flow."
Those are too large to debug.

A bounded task has three inspectable properties:

- **Known input:** The study begins from a workload slice, design question,
  simulator configuration, benchmark version, or review packet.
- **Inspectable output:** The result is a ranked list, plot, rejected candidate
  set, supporting evidence record, critique, or recommendation that another
  reader can inspect.
- **Diagnosable failure:** If the study produces an incorrect or unsupported result,
  the record shows whether the problem was the task, representation, tool
  wrapper, method, evidence, or human instruction.

The executed study in @sec-executed-scale-study is appropriately small for a
first task. It fixes one three-layer GEMM workload, one simulator configuration,
one legal array-shape family, one baseline, two equal-budget comparison arms,
and one mechanism probe. Its output includes a tie, dominated alternatives, a
falsified explanation, and a replay package. Start by replaying that study;
then change one boundary, such as the workload or legal geometry, and treat the
change as a new study rather than silently revising the old result. The
[companion lab](https://github.com/harvard-edge/arch2/tree/main/labs/examples/ai_systolic_array_study)
provides the executable path.

## Choose a Representation

The representation defines the information and artifacts available to the
study's human and automated participants. A minimal representation may include
configuration files, workload traces, simulator outputs, architecture
descriptions, scripts, plots, notes, constraints, and prior rejected candidates.
It does not have to be perfect, but the study must preserve its contents and
version so another architect can reconstruct what the method could see and
change.

Before running anything, record four boundaries around the representation:

- What the participants may read.
- What the participants may write.
- What the participants must not change.
- What assumptions live outside the representation.

The last item matters. Early studies often fail because important state is
outside the represented boundary, such as a simulator default, a benchmark version, an undocumented
constraint, a hidden preprocessing step, a fragile script, or a human judgment
that never gets recorded. For example, if an AI generates an accelerator
configuration but the area constraint is held only in the architect's head, it
can propose invalid, oversized designs. Exposing that missing constraint is a
useful result because it prevents the constraint from remaining a hidden input
to a larger study.

## Wrap the Environment

An environment wrapper defines the actions a method can take, the observations
it receives, the constraints it must obey, the cost of each evaluation, and the
provenance recorded for each run. For a first wrapper, keep the interface narrow
enough to trace every action and outcome:

- a small action space, such as a few tunable architecture parameters;
- a fixed workload or small workload set;
- checks that reject invalid actions before they reach a tool;
- one inexpensive observation and one challenge check that could count against
  the claim;
- logged tool versions, seeds, configurations, and errors;
- a run directory that preserves successful and failed attempts.

The wrapper should record failures. A hard-constraint violation can reject
a candidate. A compile error, timeout, stale benchmark, or incomplete log should
first be recorded as an environment failure unless a declared rule makes
the candidate responsible for that outcome. For example, if a candidate with a
256~KB Level 2 (L2) cache and 128-bit vector unit, run at seed 7 under simulator
v2.3, times out after ten minutes, the log should preserve the timeout and its
context. It becomes a candidate rejection only if the study declared that limit
in advance as a constraint on candidates. A first wrapper that records failures
is more valuable than a larger wrapper that only reports successes.

## Assign the Method Role

Do not begin by asking a model or automated optimizer to do everything. Choose one method
role and name it. The role might be generation, prediction,
optimization, critique, repair, verification, explanation, or coordination.

The role should match the task and feedback budget. If evaluations are cheap,
an optimization role may be reasonable. If evaluations are expensive, critique,
explanation, or surrogate prediction may be more useful. If the
representation is incomplete or inconsistent, the first useful work may be
extracting and organizing the available state. If the tool wrapper is fragile,
the first role may be verification, used to check whether runs are valid.

A useful rule is to write the method sentence before implementing the method:

> This system will act as a *role* that takes *inputs*, is allowed to
> perform *actions*, receives *feedback*, and produces
> *evidence for the named decision owner to review*.

If that sentence cannot be completed, the study is not ready for method work.

## Predeclare Evidence, Explanation, and Decision Rules

Before the first run, state what evidence is enough, what evidence is not
enough, and what forces rejection or escalation. This is the smallest version
of the decision rules in @sec-feedback-verification-trust.

Use rules such as these to connect each result to the evidence and authority
needed to act on it:

- A proxy estimate can rank candidates within a calibrated boundary but cannot
   justify a conclusion beyond that boundary.
- A claim that explains *why* a result occurred must name a mechanism
   hypothesis, a directional prediction, and a sensitivity, intervention, or
   ablation that could distinguish that account from a competing one.
- A claim that AI improved the study needs a matched conventional or no-AI
   comparison unless the claim is limited to feasibility or use.
- A simulator result is valid only if the workload version, seed,
   configuration, and tool version are logged.
- A candidate is rejected if it violates a hard constraint. A failed run is
   recorded as an environment failure, while a multiobjective tradeoff remains
   for review under the declared decision rule.
- A result is routed to a more decision-relevant or independent check only after
   it passes the earlier declared checks and preserves its assumptions.
- Any more consequential transition requires approval under a declared decision
   right. Routine checks may proceed under delegated authority, while named
   people or authorized teams decide exceptions, waivers, and consequential
   transitions.

An automated result becomes evidence only when the study records its
observations, provenance, scope, and acceptance or rejection criteria. The
design-loop card records these rules in the Rejection checks and authority,
Evidence-supported claim boundary, and Accountable decision fields.

## Fill in the Minimal Design-Loop Card

@sec-appendix-b-design-loop-card gives the full design-loop card and review
rubric. For a first bootstrap pass, transfer the outputs from
@tbl-silicon-playbook into the card before running the study, then revise the
same card after the first run. The card serves as the study summary before and
after that run. Three questions are enough to test whether the first card is
ready.

1. What question, baseline, alternatives, constraints, claim, and non-goals
   bound the study?
2. What artifact and tool path will test the claim, and which observation will
   distinguish the proposed mechanism from a competing account?
3. What bounded recommendation could the evidence support, what would overturn
   it, and which role is responsible for the decision?

The worked example below applies those questions to one decision.

```{=latex}
\Needspace{30\baselineskip}
```

::: {.callout-note title="Replay one bounded decision before extending it" icon=false}
**Context.** The study asks which legal systolic-array geometry should advance
for one frozen workload and what mechanism explains the result.

**AI role.** The model may propose three geometries and a falsifiable mechanism.
It may not run SCALE-Sim, waive a rejection check, recommend a candidate, or authorize a next
action.

**Boundary.** The study compares computed cycles and utilization under one
SCALE-Sim configuration. It makes no energy, area, compiler, RTL, physical-
design, or product claim.

**Result.** The model and conventional arms tie, the proposed mechanism is
falsified, and the evidence supports retaining the baseline and stopping. The
named owner decides whether to act on that recommendation.
:::

The first pilot should fit within a short review cycle and at most an afternoon
of execution. @tbl-first-pilot-agenda gives each step one output and a time
limit.

| **Pilot step** | **Output** | **Timebox** |
| --- | --- | --- |
| **Pick one decision** | A single architecture question with an owner and a non-goal. | 15 minutes. |
| **Draft the study brief** | Intent, bounded task, baseline, alternatives, and legal, invalid, and deferred design choices. | 30 minutes. |
| **Run one cheap check** | One proxy, replay, compile, simulator, or review result with provenance. | One afternoon or less. |
| **Preserve one failure** | A rejected candidate, invalid action, missing input, or tool error with reason. | During the first run. |
| **Review the recommendation** | A note that states the mechanism status and whether the evidence supports exploration, implementation, escalation, or rejection. | 15 minutes. |

: **A first pilot should be small enough to finish.** It should produce a
readable account of one successful check, one failure, and the bounded
recommendation that follows from both. Expanding automation can wait until
another architect can review that account. {#tbl-first-pilot-agenda tbl-colwidths="[22,46,22]"}

After the first run, evaluate the study's execution path against five post-run
checks:

- **Readable record:** Another architect can understand what happened.
- **Complete run record:** Successful and failed attempts are preserved.
- **Evidence fit:** The evidence supports the proposed recommendation or next
  action.
- **Challenge behavior:** A relevant challenge check can confirm, narrow, or
  reject the claim.
- **Revision target:** The record identifies what should change first among the
  task, representation, environment, method, explanation, evidence rule, or
  decision process.

If the execution path fails to produce a readable record, repair the run record
before adding more AI participants. If no declared observation could count
against the result, the output cannot support a decision. Add a relevant
challenge condition. The simplest credible bootstrap study records its evidence,
mechanism status, and failure modes so the team can identify the next revision;
the amount of automation is secondary. @sec-appendix-b-design-loop-card provides a blank card and review rubric that
can be reused for papers, proposals, design projects, and design reviews.
